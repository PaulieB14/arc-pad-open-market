# Server-only (Vercel / local vercel dev) — used by api/graphql.ts demo proxy
# Never prefix with VITE_ — that would expose it to the browser bundle.
# GRAPH_STUDIO_API_KEY=

# Optional client-side default for local Vite (prefer pasting in the UI)
# VITE_GRAPH_API_KEY=
